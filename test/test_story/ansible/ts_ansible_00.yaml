###################
# time:
# spec:
# author:
###################

schema:
  steps:
    -
      type: monitor
      service: 
        name: ansible
        call: REST
      action: run
      args:
        command: start
        target:
          ip: 192.168.199.130
          port: 8080
        params:
          - cpu
          - memory
          - disk

    -
      type: workload
      service: ansible
      call: REST
      args:
        command: workload
        target:
          ip: 192.168.199.130
          port: 8080
        domain: make_a_phonecall
        amount: 100
        duration: 60
